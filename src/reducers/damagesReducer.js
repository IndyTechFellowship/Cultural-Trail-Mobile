import * as types from '../actions/actionTypes';

const initialState = {
    damageTypes: null,
};

export default function counter(state = initialState, action = {}) {
    switch (action.type) {
        default:
            return state;
    }
}